import React from 'react'
import { View, Text } from 'react-native'

export default function Weather({navigation}) {
    return (
        <View>
            <Text>Weather</Text>
        </View>
    )
}
